spring:
  application:
    name: demo-api
  profiles:
    active: ${profiles.active}
  cloud:
    config:
      name: demo-profile,demo-api
      #profile: dev
      #label: master
      uri: ${spring.cloud.config.uri}
      
